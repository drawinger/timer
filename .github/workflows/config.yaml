name: build

on: push
 
# Тут можно конкретизировать ветку, а так все
#   push:
#    branches:
#      - locify-github-actions

jobs:
  deploy:
    runs-on: "ubuntu-latest123"

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: check files
        run: ls -la

#      - name: running linters
#        run: npm i && npm run lint-fix
